{
  "name": "fractal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd Fractal-Vue && npm run dev",
    "build": "cd Fractal-Vue && npm run build",
    "preview": "cd Fractal-Vue && npm run preview",
    "lint": "cd Fractal-Vue && npm run lint"
  }
}
