{
  "_comment": "Copy this file as `.cursor/mcp.json` (Cursor) or merge into your client's MCP config. The package is on PyPI (https://pypi.org/project/fractal-mcp/); `uvx` will download and cache it on first run. Replace the agent token. See README and docs/agent/AGENT_QUICKSTART.md for how to issue the token.",
  "mcpServers": {
    "fractal": {
      "command": "uvx",
      "args": ["fractal-mcp"],
      "env": {
        "FRACTAL_BASE_URL": "http://localhost:8888",
        "FRACTAL_AGENT_TOKEN": "qd_agent_REPLACE_ME"
      }
    }
  }
}
